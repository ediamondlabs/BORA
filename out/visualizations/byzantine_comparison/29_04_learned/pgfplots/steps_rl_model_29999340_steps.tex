% ============================================================
% Add these packages to your LaTeX preamble (if not present):
%   \usepackage{pgfplots}
%   \usepackage{pgfplotstable}
%   \usepackage{siunitx}
%   \usepgfplotslibrary{fillbetween}
%   \pgfplotsset{compat=1.18}
% ============================================================

\definecolor{colorLearnedDirLie}{HTML}{E41A1C}

\pgfplotsset{
    % Shared styles
    ci path/.style={draw=none},
    ci fill/.style={opacity=0.15},
    mean/.style={thick, mark repeat=200, mark size=1.5pt},
    % Per-condition styles
    LearnedDirLie/.style={color=colorLearnedDirLie, mark=*},
}

\begin{tikzpicture}
    \begin{axis}[
        grid=major,
        grid style={dashed, gray!30},
        width=\linewidth,
        xlabel={\small $t$},
        ylabel={\small $\tau$ [\si{\bit\per\second}]},
        legend style={at={(0.5,-0.20)}, anchor=north, legend columns=3},
        tick label style={font=\small},
        label style={font=\small},
    ]

    % ===== Learned Dir Lie =====
    \addplot [name path=LearnedDirLie upper, ci path, forget plot] table [x=x, y=upLearnedDirLie, col sep=comma] {steps_rl_model_29999340_steps.csv};
    \addplot [name path=LearnedDirLie lower, ci path, forget plot] table [x=x, y=lowLearnedDirLie, col sep=comma] {steps_rl_model_29999340_steps.csv};
    \addplot [LearnedDirLie, ci fill, forget plot] fill between [of=LearnedDirLie upper and LearnedDirLie lower];
    \addplot [LearnedDirLie, mean] table [x=x, y=meanLearnedDirLie, col sep=comma] {steps_rl_model_29999340_steps.csv};
    \addlegendentry{Learned Dir Lie}

    \end{axis}
\end{tikzpicture}